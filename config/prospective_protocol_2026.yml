protocol_version: "2026.1"
season: 2026
frozen_at_utc: "2026-08-28T00:38:00Z"
purpose: "Prospective paper validation of the frozen 2026 CFB totals research rules."

rules:
  general:
    model_track: "GENERAL HGB"
    direction: "under"
    qualify_edge_points: 3.5
    minimum_total: 56.0
  fcs:
    model_track: "FCS-only HGB"
    direction: "under"
    qualify_edge_points: 7.5
    minimum_total: 56.0

official_entry_policy:
  eligible_event_name: "schedule"
  eligible_crons:
    - "0 14 * * 4"
    - "0 14 * * 5"
    - "0 13 * * 6"
  minimum_lead_minutes: 120
  selection: "For each game, use the latest successful eligible scheduled snapshot at least 120 minutes before kickoff. For duplicate attempts of the same GitHub run, retain the earliest successful attempt. Manual and push-triggered refreshes are archived but are never official entries."

closing_benchmark_policy:
  capture_window_minutes: 90
  selection: "Use the latest immutable market capture before kickoff. Prefer the median total across available books; if a median is unavailable, use the selected current total."
  clv_definition: "For an under, CLV points = entry total minus benchmark close total. Positive is favorable. For an over, CLV points = benchmark close total minus entry total."

paper_grading:
  price: -110
  qualifying_status: "QUALIFIES"
  unit_stake: 1.0
  win_profit_units: 0.9090909091
  loss_profit_units: -1.0
  push_profit_units: 0.0

freeze_policy:
  description: "Do not tune production thresholds or retrospectively alter official-entry selection based on 2026 outcomes before the declared review point. Any future protocol change must receive a new protocol_version and applies prospectively only."
  review_point: "After the 2026 regular season unless a data-integrity bug requires a documented correction."
