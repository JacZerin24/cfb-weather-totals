# FCS model reproducibility

The live FCS research track pins pandas, numpy, and scikit-learn in `requirements.txt` so HistGradientBoosting research outputs are reproducible across GitHub Actions refreshes. Candidate FCS under screens are compared season-by-season before changing the live `QUALIFIES` threshold.
